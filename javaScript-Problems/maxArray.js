var marks = [33, 99, 45, 20, 32, 92];

var max = marks[0];

for (var i = 0 ; i<marks.length ; i++){

    var elements = marks[i];

    if( elements > max ){


        max = elements 

    }
}

console.log( 'Array Large  Number is ',  max )
